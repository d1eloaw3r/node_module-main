<!DOCTYPE html>
<html>
<head>
  <title>Мои заявки</title>
  <link rel="stylesheet" href="/bootstrap/css/bootstrap.min.css"> 
  <link rel="stylesheet" href="../public/style.css">
</head>
<body>
  <div class="container py-4">
    <div class="d-flex justify-content-between align-items-center mb-3">
      <h1>Личный кабинет</h1>
      <a href="/form" class="btn btn-primary">+ Новая заявка</a>
    </div>
    <h2 class="h5">Ваши заявки:</h2>
    {{#if apps.length}}
      <div class="row g-3">
        {{#each apps}}
          <div class="col-12 col-md-6 col-lg-4">
            <div class="card h-100">
              <div class="card-body">
                <p class="mb-1"><strong>Марка:</strong> {{car_brand}}, <strong>Модель:</strong> {{car_model}}</p>
                <p class="mb-1"><strong>Дата:</strong> {{desired_date}}, <strong>Время:</strong> {{desired_time}}</p> {{!-- ОБНОВЛЕННАЯ СТРОЧКА --}}
                <p class="mb-1"><strong>Статус:</strong> {{status}}</p>
                {{#ifEquals status "отклонено"}}
                  <p class="mb-1 text-danger"><strong>Причина:</strong> {{rejection_reason}}</p>
                {{/ifEquals}}
              </div>
            </div>
          </div>
        {{/each}}
      </div>
    {{else}}
      <p>У вас пока нет заявок.</p>
    {{/if}}
    <a href="/logout" class="btn btn-outline-secondary mt-4">Выйти</a>
  </div>
</body>
</html>
